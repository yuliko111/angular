import { FilterHotelsPipe } from './filter-hotels.pipe';

describe('FilterHotelsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHotelsPipe();
    expect(pipe).toBeTruthy();
  });
});
